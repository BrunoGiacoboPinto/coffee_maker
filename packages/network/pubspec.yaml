name: network
description: Network connectivity and HTTP client utilities for Coffee Maker app
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  bloc: ^9.0.1
  connectivity_plus: ^6.0.5
  dio: ^5.7.0
  environment:
    path: ../environment
  injectable: ^2.5.0
  logging: ^1.3.0

dev_dependencies:
  bloc_test: ^10.0.0
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
